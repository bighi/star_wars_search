services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
    volumes:
      - .:/app
      - /app/node_modules
  jobs:
    build: .
    command: ["node", "src/jobs/index.ts"]
    restart: unless-stopped
    volumes:
      - .:/app
      - /app/node_modules
